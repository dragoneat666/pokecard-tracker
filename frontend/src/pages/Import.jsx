// pages/Import.jsx — Excel import page (placeholder for step 6)

export default function Import() {
  return (
    <div style={{ maxWidth: 700, margin: '0 auto' }}>
      <h1 style={{ marginBottom: 'var(--space-4)' }}>Import from Excel</h1>
      <div className="panel" style={{ color: 'var(--text-secondary)', textAlign: 'center', padding: 'var(--space-7)' }}>
        <div style={{ fontSize: '2rem', marginBottom: 'var(--space-3)' }}>📊</div>
        <div style={{ fontWeight: 700, marginBottom: 'var(--space-2)' }}>Coming in step 6</div>
        <div style={{ fontSize: '0.875rem' }}>
          Upload your existing Excel collection spreadsheets here to migrate your data.
        </div>
      </div>
    </div>
  );
}
