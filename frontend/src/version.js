// version.js — Single source of truth for the app version shown in Admin.
//
// Bump the patch digit here on every push so a deploy landing can be
// confirmed by checking this number in the Admin page.

export const APP_VERSION = '0.7.2';
